// CJS entry point for electron-builder — delegates to main.js
// The electron/ folder has its own package.json with "type":"commonjs"
// so this file is treated as CommonJS by Node automatically.
require('./main.js')
